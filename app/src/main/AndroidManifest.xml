<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".runtime.WalleeApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="${APP_NAME}"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSplash"
        tools:targetApi="33">

        <profileable
            android:shell="true"
            tools:targetApi="q" />

        <activity
            android:name=".runtime.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- AS Team product deep link. Keep this synchronized with BASE_DEEPLINK. -->
                <data
                    android:host="com.asteam"
                    android:scheme="kharjyar" />
            </intent-filter>
        </activity>

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.wisnu.kurniawan.wallee.runtime.initializer.DebugToolsInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="com.wisnu.kurniawan.wallee.runtime.initializer.ActivityLifecycleLoggrInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>

</manifest>
